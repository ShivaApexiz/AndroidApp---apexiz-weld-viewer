{
  "name": "apexiz-weld-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Android wrapper and web assets for the Apexiz Weld Viewer.",
  "scripts": {
    "sync:web": "powershell -ExecutionPolicy Bypass -File scripts/sync-web.ps1",
    "cap:sync": "npm run sync:web && npx cap sync android",
    "cap:copy": "npm run sync:web && npx cap copy android",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
